package model;

public enum ConnectionStatusEnum
{
    OFFLINE,
    CONNECTING,
    CONNECTED,
    DISCONNECTING,
    DISCONNECTED,
    WRONG_PORT,
    SERVER_START,
    SERVER_STARTED,
    SERVER_CLOSING
}
